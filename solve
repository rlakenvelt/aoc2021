FILTER=$1
if [ "$FILTER" == "" ]
then
    FILTER='';
fi
for file in `find src -name "*.ts" | grep day${FILTER}  | sort`
do
    FILENAME=`basename $file`
    DIRNAME=`dirname $file`
    cd $DIRNAME
    ts-node $FILENAME
    cd - > /dev/null
done
